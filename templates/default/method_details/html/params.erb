<% if object[:params] %>
<% params = object[:params].uniq %>
<div class="tags">
  <p class="tag_title">Params</p>
  <ul class="params">
    <% params.each do |p| %>
		<li>params[<%= link_object(p.gsub(':','').gsub("'",'').capitalize, p)%>]</li>
    <% end %>
  </ul>
</div>
<% end %>
